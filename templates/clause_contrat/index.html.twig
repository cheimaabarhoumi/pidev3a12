{% extends 'base.html.twig' %}

{% block title %}Clauses du Contrat{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h4>📋 Clauses du Contrat</h4>
            <a href="{{ path('app_clause_contrat_new', {'contratId': contratId}) }}" class="btn btn-light">
                ➕ Ajouter une Clause
            </a>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="thead-dark">
                        <tr>
                            <th>Ordre</th>
                            <th>Titre</th>
                            <th>Type</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for clause in clauses %}
                        <tr>
                            <td>{{ clause.ordre }}</td>
                            <td>{{ clause.titre }}</td>
                            <td>
                                <span class="badge bg-info">
                                    {{ clause.type }}
                                </span>
                            </td>
                            <td>
                                <a href="{{ path('app_clause_contrat_show', {'id': clause.idClause}) }}" class="btn btn-sm btn-info" title="Voir">
                                    👁️ Voir
                                </a>
                                <a href="{{ path('app_clause_contrat_edit', {'id': clause.idClause}) }}" class="btn btn-sm btn-warning" title="Modifier">
                                    ✏️ Modifier
                                </a>
                                {{ include('clause_contrat/_delete_form.html.twig', {'clause': clause}) }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="text-center text-muted">Aucune clause trouvée.</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
           <a href="{{ path('app_contrat_show', {'idContrat': contratId}) }}" class="btn btn-secondary">
            🔙 Retour au Contrat
            </a>


        </div>
    </div>
</div>
{% endblock %} 